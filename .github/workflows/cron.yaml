name: Cron  Workflow
on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Cron Manual"
      - name: Print date
        run: date